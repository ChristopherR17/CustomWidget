/// Paleta de colores de la aplicación.
/// Define todos los colores utilizados en la interfaz de usuario.

import 'package:flutter/material.dart';

/// Clase que define la paleta de colores estándar de la aplicación.
/// Utiliza colores con valores hexadecimales consistentes.
class AppPalette {
  /// Color primario de la aplicación (púrpura).
  static const Color primary = Color(0xFF7C3AED);
  
  /// Color primario oscuro para variantes.
  static const Color primaryDark = Color(0xFF312E81);
  
  /// Color secundario o de acento (cian).
  static const Color accent = Color(0xFF06B6D4);
  
  /// Color oscuro para la barra lateral.
  static const Color sidebar = Color(0xFF111827);
  
  /// Color de tarjetas en la barra lateral.
  static const Color sidebarCard = Color(0xFF1F2937);
  
  /// Color de fondo principal.
  static const Color background = Color(0xFFF8FAFC);
  
  /// Color de superficie para elementos principales.
  static const Color surface = Colors.white;
  
  /// Color púrpura suave para destacados.
  static const Color softPurple = Color(0xFFF3E8FF);
  
  /// Color cian suave para destacados.
  static const Color softCyan = Color(0xFFE0F7FA);
  
  /// Color indicador de permiso otorgado.
  static const Color permissionColor = Color(0xFFDCFCE7);
  
  /// Color indicador de permiso denegado.
  static const Color noPermissionColor = Color(0xFFFFE4E6);
  
  /// Color de texto oscuro principal.
  static const Color textDark = Color(0xFF0F172A);
  
  /// Color de texto atenuado o secundario.
  static const Color textMuted = Color(0xFF64748B);
}
